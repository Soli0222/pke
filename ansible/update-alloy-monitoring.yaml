---
- name: Update host Alloy monitoring one node at a time
  hosts: k3s_cluster
  become: true
  serial: 1
  any_errors_fatal: true
  tasks:
    - name: Update monitoring configuration without reinstalling Alloy
      ansible.builtin.include_role:
        name: install-alloy
        tasks_from: update-monitoring
