apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: external-dns-config
  namespace: flux-system
spec:
  interval: 10m
  path: "./flux/clusters/natsume/apps/external-dns-config"
  prune: true
  wait: true
  timeout: 10m
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
  - name: external-dns
