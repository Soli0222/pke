apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: plane-cluster
  namespace: plane
spec:
  instances: 2
  primaryUpdateMethod: switchover
  smartShutdownTimeout: 60
  storage:
    size: 5Gi
  bootstrap:
    initdb:
      database: plane
      owner: plane
