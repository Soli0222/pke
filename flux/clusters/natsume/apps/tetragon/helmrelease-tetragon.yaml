apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tetragon
  namespace: tetragon
spec:
  interval: 10m
  releaseName: tetragon
  targetNamespace: tetragon
  install:
    createNamespace: false
    crds: Create
  upgrade:
    crds: CreateReplace
  chart:
    spec:
      chart: tetragon
      version: 1.7.0
      sourceRef:
        kind: HelmRepository
        name: tetragon-repo
        namespace: tetragon
      interval: 1h
  values:
    tetragon:
      clusterName: natsume
      exportFileMaxSizeMB: 50
      exportFileMaxBackups: 10
      exportAllowList: |-
        {"event_set":["PROCESS_KPROBE"]}
        {"event_set":["PROCESS_TRACEPOINT"]}
        {"namespace":["misskey"],"event_set":["PROCESS_EXEC","PROCESS_EXIT"]}
      exportDenyList: |-
        {"health_check":true}
        {"namespace":["tetragon"]}
      exportRateLimit: -1
      enableProcessCred: true
      enableProcessNs: true
      processAncestors:
        enabled: base,kprobe,tracepoint
      cri:
        enabled: true
        socketHostPath: /run/k3s/containerd/containerd.sock
      prometheus:
        serviceMonitor:
          enabled: true
    tetragonOperator:
      prometheus:
        serviceMonitor:
          enabled: true
    export:
      mode: stdout
