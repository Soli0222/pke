apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: misskey-cluster-daily-backup
  namespace: misskey
spec:
  schedule: "0 30 16 * * *"
  backupOwnerReference: self
  cluster:
    name: misskey-cluster
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
