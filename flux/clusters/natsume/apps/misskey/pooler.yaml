apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: misskey-pooler-rw
  namespace: misskey
spec:
  cluster:
    name: misskey-cluster
  instances: 2
  type: rw
  pgbouncer:
    poolMode: session
    parameters:
      max_client_conn: "200"
      default_pool_size: "30"
      reserve_pool_size: "10"
      reserve_pool_timeout: "3"
      server_idle_timeout: "120"
      server_lifetime: "3600"
      query_wait_timeout: "30"
      ignore_startup_parameters: "statement_timeout"
  template:
    spec:
      containers: []
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchLabels:
                  cnpg.io/poolerName: misskey-pooler-rw
              topologyKey: kubernetes.io/hostname
