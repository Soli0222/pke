apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: uptime-kuma
  namespace: uptime-kuma
spec:
  interval: 10m
  releaseName: uptime-kuma
  targetNamespace: uptime-kuma
  install:
    createNamespace: false
    crds: Create
  upgrade:
    crds: CreateReplace
  chart:
    spec:
      chart: uptime-kuma
      version: 4.1.0
      sourceRef:
        kind: HelmRepository
        name: uptime-kuma-repo
        namespace: uptime-kuma
      interval: 1h
  values:
    volume:
      enabled: true
      accessMode: ReadWriteOnce
      size: 4Gi
    ingress:
      enabled: true
      className: traefik
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-http01
      hosts:
      - host: status.pstr.space
        paths:
        - path: "/"
          pathType: ImplementationSpecific
      tls:
      - hosts:
        - status.pstr.space
        secretName: status.pstr.space-http01
    serviceMonitor:
      enabled: false
