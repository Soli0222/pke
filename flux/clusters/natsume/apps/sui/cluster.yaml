apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: sui-cluster
  namespace: sui
spec:
  instances: 1
  storage:
    size: 5Gi
  bootstrap:
    initdb:
      database: sui
      owner: sui
