apiVersion: v1
kind: Service
metadata:
  name: {{ include "summaly.fullname" . }}
  labels:
    {{- include "summaly.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "summaly.selectorLabels" . | nindent 4 }}
