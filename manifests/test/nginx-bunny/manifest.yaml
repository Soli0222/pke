---
apiVersion: v1
kind: Pod
metadata:
  labels:
    run: nginx-bunny
  name: nginx-bunny
  namespace: default
spec:
  containers:
  - image: nginx:1.28
    name: nginx-bunny
    resources:
      limits:
        cpu: 200m
        memory: 731Mi
      requests:
        cpu: 100m
        memory: 512Mi
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
---
apiVersion: v1
kind: Service
metadata:
  labels:
    run: nginx-bunny
  name: nginx-bunny
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    run: nginx-bunny
status:
  loadBalancer: {}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
    external-dns.alpha.kubernetes.io/target: nginx-bunny-pstr-space.b-cdn.net
  name: nginx-bunny
  namespace: default
spec:
  ingressClassName: traefik-external
  rules:
  - host: nginx-bunny.pstr.space
    http:
      paths:
      - backend:
          service:
            name: nginx-bunny
            port:
              number: 80
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - nginx-bunny.pstr.space
    secretName: nginx-bunny.pstr.space-dns01