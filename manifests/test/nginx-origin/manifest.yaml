---
apiVersion: v1
kind: Pod
metadata:
  labels:
    run: nginx-origin
  name: nginx-origin
  namespace: default
spec:
  containers:
  - image: nginx:1.28
    name: nginx-origin
    resources:
      limits:
        cpu: 200m
        memory: 731Mi
      requests:
        cpu: 100m
        memory: 512Mi
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
---
apiVersion: v1
kind: Service
metadata:
  labels:
    run: nginx-origin
  name: nginx-origin
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    run: nginx-origin
status:
  loadBalancer: {}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
    external-dns.alpha.kubernetes.io/target: conohav2-1.pstr.space
  name: nginx-origin
  namespace: default
spec:
  ingressClassName: traefik-external
  rules:
  - host: nginx-origin.pstr.space
    http:
      paths:
      - backend:
          service:
            name: nginx-origin
            port:
              number: 80
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - nginx-origin.pstr.space
    secretName: nginx-origin.pstr.space-dns01