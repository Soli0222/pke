ingress:
  enabled: true
  ingressClassName: "traefik"
  host: "renovate.str08.net"
  tls:
    - hosts:
        - "renovate.str08.net"
      secretName: "renovate.str08.net-dns01"
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt"

metrics:
  enabled: true
  serviceMonitor:
    enabled: true