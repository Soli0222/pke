apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: peertube-cluster
spec:
  instances: 3
  storage:
    size: 10Gi
  bootstrap:
    initdb:
      database: peertube
      owner: peertube
