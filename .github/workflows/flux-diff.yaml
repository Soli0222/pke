name: flux-diff

on:
  pull_request:
    paths:
      - "flux/**"
      - "charts/**"
      - ".github/workflows/flux-diff.yaml"

permissions:
  contents: read
  pull-requests: write

env:
  # helm Capabilities.APIVersions。CRD が入っている前提で render する chart 用。
  API_VERSIONS: policy/v1/PodDisruptionBudget,monitoring.coreos.com/v1

jobs:
  diff:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        cluster: [natsume, meruto]
        resource: [hr, ks]
    steps:
      - name: Check out the PR branch
        uses: actions/checkout@v7.0.1
        with:
          path: pr

      - name: Check out the base branch
        uses: actions/checkout@v7.0.1
        with:
          ref: ${{ github.event.pull_request.base.ref }}
          path: live

      - name: Set up flate
        uses: home-operations/flate/action@v0.6.5
        with:
          base: ""
          cache: true

      # changed-only モード (flate の --path-orig / --base) は使わない。
      # Kustomization の spec.path に紐づかないファイルの変更が keep set に
      # 入らないため (home-operations/flate#833)、リポジトリ直下の
      # charts/ を GitRepository 経由で参照している HelmRelease の変更が
      # 無言で落ちる。両リビジョンを常にフルレンダリングして比較する。
      - name: Render both revisions
        env:
          CLUSTER: ${{ matrix.cluster }}
          RESOURCE: ${{ matrix.resource }}
        run: |
          set -euo pipefail
          # setup-flate は base 入力 (既定はデフォルトブランチ) を FLATE_BASE として
          # export する。これが残っていると changed-only モードに落ちるので外す。
          unset FLATE_BASE
          for side in live pr; do
            (
              cd "$side"
              flate build "${RESOURCE}" \
                --path "flux/clusters/${CLUSTER}" \
                --api-versions "${API_VERSIONS}" \
                --skip-secrets=true \
                --no-progress
            ) > "${side}.yaml"
          done

      - name: Compose comment
        id: comment
        env:
          CLUSTER: ${{ matrix.cluster }}
          RESOURCE: ${{ matrix.resource }}
          BASE_REF: ${{ github.event.pull_request.base.ref }}
          GH_TOKEN: ${{ secrets.FLUX_DIFF_GIST_TOKEN || secrets.GITHUB_TOKEN }}
          HAS_GIST_TOKEN: ${{ secrets.FLUX_DIFF_GIST_TOKEN != '' }}
          PUBLIC_REPO: ${{ github.event.repository.private == false }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          # GitHub PR コメントは 65536 文字上限。コードフェンスや見出し分の余白を残す。
          INLINE_LIMIT=60000
          delimiter="$(openssl rand -hex 8)"

          if diff -u --label "live (${BASE_REF})" --label "pr" \
              live.yaml pr.yaml > diff.patch; then
            {
              echo "message<<${delimiter}"
              echo "_\`${CLUSTER}\` の ${RESOURCE} に差分はありません。_"
              echo "${delimiter}"
            } >> "$GITHUB_OUTPUT"
            exit 0
          fi

          size=$(wc -c < diff.patch)

          if (( size <= INLINE_LIMIT )); then
            {
              echo "message<<${delimiter}"
              echo "<details><summary>flate <code>${CLUSTER}</code> / <code>${RESOURCE}</code> diff (${size} bytes)</summary>"
              echo
              echo '```diff'
              cat diff.patch
              echo '```'
              echo "</details>"
              echo "${delimiter}"
            } >> "$GITHUB_OUTPUT"
          elif [[ "$HAS_GIST_TOKEN" == true ]] && gist_url=$(gh gist create diff.patch \
              --desc "flate ${CLUSTER}/${RESOURCE} diff for ${REPO}#${PR_NUMBER}" \
              | tail -n1); then
            {
              echo "message<<${delimiter}"
              echo "flate \`${CLUSTER}\` / \`${RESOURCE}\` diff は **${size} bytes** で 65536 字制限を超えるため Gist に退避しました: ${gist_url}"
              echo "${delimiter}"
            } >> "$GITHUB_OUTPUT"
          else
            # 公開コードから生成した差分のみを同じ repository の artifact に保存する。
            # repository を非公開に変更した場合は、自動的にこの fallback を使わない。
            if [[ "$PUBLIC_REPO" != true ]]; then
              echo "::error::Large diff requires a configured Gist token in a private repository"
              exit 1
            fi
            {
              echo "upload_artifact=true"
              echo "message<<${delimiter}"
              echo "flate \`${CLUSTER}\` / \`${RESOURCE}\` diff は **${size} bytes** です。Gist を利用できないため、この [Actions run](${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}) の \`flate-${CLUSTER}-${RESOURCE}-diff\` artifact に保存しました。"
              echo "${delimiter}"
            } >> "$GITHUB_OUTPUT"
          fi

      - name: Upload large diff when Gist is unavailable
        if: steps.comment.outputs.upload_artifact == 'true'
        uses: actions/upload-artifact@v7.0.1
        with:
          name: flate-${{ matrix.cluster }}-${{ matrix.resource }}-diff
          path: diff.patch
          retention-days: 14
          if-no-files-found: error

      - name: Sticky PR comment
        uses: marocchino/sticky-pull-request-comment@v3.0.5
        with:
          header: flux-diff/${{ matrix.cluster }}/${{ matrix.resource }}
          message: ${{ steps.comment.outputs.message }}
